[project]
name = "esp32-mqtt"
version = "0.1.0"
description = "MQTT test tools: host-side paho-mqtt scripts and an ESP32 MicroPython device script"
requires-python = ">=3.9"
dependencies = [
    "paho-mqtt==2.1.0",
]
